[pytest]
markers =
    requires_macos: tests that shell out to system_profiler/ioreg and only run on real macOS hardware
    requires_sudo: tests that need elevated privileges
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
